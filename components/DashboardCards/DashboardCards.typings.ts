import { ReactElement } from "react";
import { IconType } from "react-icons/lib";

export interface ICard {
    color: string;
    bg: string;
    amount: string;
    description: string;
    Icon: IconType;
}
